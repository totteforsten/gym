{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "export DATABASE_URL_UNPOOLED=\"${DATABASE_URL_UNPOOLED:-$DATABASE_URL}\" && prisma generate && prisma db push --skip-generate && next build",
  "framework": "nextjs"
}
